# Inops dummy shop – all required env vars (Vite exposes only VITE_*)
# Copy to .env for CI/build; use .env.local for local dev (overrides .env).

# Required: Inops API base URL. For local dev use localhost (or SDK falls back to production).
# e.g. http://localhost:3000 or http://127.0.0.1:3000
VITE_INOPS_API_BASE_URL=http://localhost:3000

# Required: SearchKey for this shop (from merchant portal → Security)
VITE_INOPS_SEARCH_KEY=

# Required for campaign block: campaign referenceId (from merchant portal → Campaigns)
VITE_INOPS_CAMPAIGN_ID=

# Optional: SaaS homepage for "Powered by" links
VITE_INOPS_HOMEPAGE_URL=https://www.inops.io

# Optional: Shop name in header/footer
VITE_DUMMY_SHOP_NAME=Longboard Dummy Shop

# Optional: CI/build only (tenant build sets this); leave unset for local dev
# VITE_SKIP_AUTH=true
