# Inops dummy Shopify shop (local dev)
#
# IMPORTANT:
# - Vite only exposes env vars prefixed with VITE_
# - SearchKey + campaignId MUST belong to the same shopConfig in the backend
#

# Local backend - placeholder will be replaced during deployment
VITE_INOPS_API_BASE_URL=TENANT_INJECT_VITE_INOPS_API_BASE_URL

# Demo auth (public bearer key) - placeholder will be replaced during deployment
VITE_INOPS_SEARCH_KEY=TENANT_INJECT_VITE_INOPS_SEARCH_KEY

# Campaign landing (merchant-defined referenceId; case-sensitive) - placeholder will be replaced during deployment
VITE_INOPS_CAMPAIGN_ID=TENANT_INJECT_VITE_INOPS_CAMPAIGN_ID

# Where all links should go (SaaS homepage) - placeholder will be replaced during deployment
VITE_INOPS_HOMEPAGE_URL=TENANT_INJECT_VITE_INOPS_HOMEPAGE_URL

# Optional branding - placeholder will be replaced during deployment
VITE_DUMMY_SHOP_NAME=TENANT_INJECT_VITE_DUMMY_SHOP_NAME

